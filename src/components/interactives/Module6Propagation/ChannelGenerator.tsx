import type { FC } from 'react';

/**
 * Channel impulse response generator for wireless scenarios.
 * TODO: Provide fading models, delay spreads, and export options.
 */
const ChannelGenerator: FC = () => (
  <div className="rounded-lg border border-dashed border-sky-400/30 bg-sky-500/5 p-4 text-sm text-sky-50">Channel Generator placeholder</div>
);

export default ChannelGenerator;
