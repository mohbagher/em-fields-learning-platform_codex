import type { FC } from 'react';

/**
 * Radar signal and target detection simulator.
 * TODO: Model pulses, Doppler shifts, and clutter filtering.
 */
const RadarSimulator: FC = () => (
  <div className="rounded-lg border border-dashed border-teal-400/30 bg-teal-500/5 p-4 text-sm text-teal-50">Radar Simulator placeholder</div>
);

export default RadarSimulator;
