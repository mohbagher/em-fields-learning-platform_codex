/**
 * Placeholder utilities for RF propagation modeling.
 * TODO: Add path loss models, fading calculators, and terrain coupling.
 */
export function samplePropagationMetric(): number {
  return 0;
}
