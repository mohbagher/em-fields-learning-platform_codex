import type { FC } from 'react';

/**
 * Simulator to show weather impacts on signal propagation.
 * TODO: Model rain fade, fog attenuation, and atmospheric absorption.
 */
const WeatherImpactSimulator: FC = () => (
  <div className="rounded-lg border border-dashed border-sky-400/30 bg-sky-500/5 p-4 text-sm text-sky-50">Weather Impact Simulator placeholder</div>
);

export default WeatherImpactSimulator;
