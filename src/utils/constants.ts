/**
 * Centralized constants for the EM Fields platform.
 * TODO: Expand with module metadata and feature flags.
 */
export const MODULE_COUNT = 8;
export const DEFAULT_THEME = 'dark';

// Physical constants
export const EPSILON_0 = 8.8541878128e-12; // vacuum permittivity (F/m)
export const K_E = 1 / (4 * Math.PI * EPSILON_0); // Coulomb's constant (N·m²/C²)
