import type { FC } from 'react';

/**
 * Simulator showing path loss over varying distances and frequencies.
 * TODO: Add terrain presets and antenna gain inputs.
 */
const LinkDistanceSimulator: FC = () => (
  <div className="rounded-lg border border-dashed border-sky-400/30 bg-sky-500/5 p-4 text-sm text-sky-50">Link Distance Simulator placeholder</div>
);

export default LinkDistanceSimulator;
